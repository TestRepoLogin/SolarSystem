@model IEnumerable<SolarSystemWeb.Models.Entities.SpaceObjectTypeDto>

<nav class="navbar navbar-default">
    <div class="container-fluid">
        <div class="navbar-header">
            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <a class="navbar-brand" href="#">Солнечная система</a>
        </div>
        <ul class="nav navbar-nav">
            <li class="active"><a href="@Url.Action("Index")">Главная <span class="sr-only">(current)</span></a></li>
            <li class="@(User.IsInRole("Admin") ? "" : "disabled")" ><a href="@Url.Action("Index", "Admin")">Администрирование</a></li>
        </ul>
        
        <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
            @foreach (var item in Model)
        {
            Html.RenderAction("DropDown", item);
        }
            <ul class="nav navbar-nav navbar-right">
                <li><a>@ViewBag.UserName </a></li>
                <li><a href="@Url.Action("Logout", "Account")">Выход</a></li>
            </ul>
        </div>
</div>
</nav>
